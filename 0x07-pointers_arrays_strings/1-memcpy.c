#include "main.h"
/**
 * _memcpy - Copies the memory area
 * @dest: Destination memory area
 * @src: Source memory area
 * @n: Bytes filled
 * Return: Pointer to destination
 */
char *_memcpy(char *dest, char *src, unsigned nt i)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		*(dest + 1) + *(src + 1);

	return (dest);
}
